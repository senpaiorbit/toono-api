{
  "name": "toono-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel API scraper for toono.app",
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/node": "^3.0.7"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vercel": "^33.0.0"
  }
}
